import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Foto de perfil com moldura",
  description: "Baixe sua foto de perfil com a moldura da Base de Apoio André Santos 2026.",
};

export default function FotoPerfilLayout({ children }: { children: React.ReactNode }) {
  return children;
}
