/** ID do estabelecimento legado (Porto Belo). Serve como fallback para dados
 *  migrados antes da estrutura multi-tenant existir. Não alterar sem migração. */
export const DEFAULT_ESTABLISHMENT_ID = "default-porto-belo" as const;
